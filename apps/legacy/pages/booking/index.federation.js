/**
 * Federation config for Booking page
 *
 * This page uses default settings - minimal shared state integration.
 */
module.exports = {
  // Uses default events: ['state:changed']
  // No shared state injection
  // No event logging
}
